#include <iostream>
#include <map>
#include <unordered_map>
#include <vector>
#include <regex>
using namespace std;

// using stringToIntMap = std::unordered_map<std::string, size_t>;
// using intToStringMap = std::unordered_map<std::size_t, vector<std::string>>;

// stringToIntMap countWords(const std::string& input) {
//     std::regex wordRegex(R"(\w+)");
//     std::smatch matches;
//     regex_search(input, matches, wordRegex);
//     cout << "Total = " << matches.size() << '\n';
//     for (auto match: matches)
//         cout << "> " << match.str() << '\n';
// }

int sum(int a, int b){
    int c = a + b;
}

int main()
{
    cout << sum(3, 5) << '\n';
    // cout<<"Counting words"<<endl;
    // string input = R"(asfasfd asfsda sdaf)";
    // auto wordToFrequencyMap = countWords(input);
    // cout<< "[[before]]" << endl;
    // for(auto [k, v]: wordToFrequencyMap){
    //     cout << k << "|" << v << endl;
    // }
    // cout<< "[[after]]" << endl;
}
